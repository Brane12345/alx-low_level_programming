#include <stdio.h>
/**
 * main - Prints the lowercase alphabets in reverse
 *
 * Return: 0 (success)
 */
int main(void)
{
	char i;

	i = 'z';
	while
		(i >= 'a') {
			putchar(i);
			i--;
		}
	putchar('\n');
	return (0);
}
